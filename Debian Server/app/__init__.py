"""Application package for the Lumina EduMesh Hub FastAPI server.

Contains the main API router (api.py), mDNS beacon (discovery.py),
the ZIM cache auto-cleaner (zim_auto_cleaner.py), and refactored
route modules in app/routers/.
"""
