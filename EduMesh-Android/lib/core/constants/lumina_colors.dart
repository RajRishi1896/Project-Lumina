import 'package:flutter/material.dart';

/// The app's colour palette with semantic colour tokens.
class LuminaColors {
  /// The background surface colour for screens and cards.
  static const Color surface = Color(0xFFFDF7FF);

  /// The primary deep blue used for app bars and emphasis.
  static const Color primaryDeepBlue = Color(0xFF1A2B44);

  /// An academic teal accent for interactive elements.
  static const Color academicTeal = Color(0xFF007083);

  /// A saffron yellow used for highlights and badges.
  static const Color saffron = Color(0xFFFFD300);

  /// A green colour for success indicators.
  static const Color successGreen = Color(0xFF38A169);

  /// A neutral outline colour for borders and dividers.
  static const Color outline = Color(0xFFE2E8F0);

  /// The default text colour on surface backgrounds.
  static const Color onSurface = Color(0xFF1D1B20);

  /// The text and icon colour on primary-coloured containers.
  static const Color onPrimary = Color(0xFFFFFFFF);

  /// A red colour for danger/delete actions.
  static const Color danger = Color(0xFFDC2626);

  /// A purple colour for chart differentiation.
  static const Color chartPurple = Color(0xFF7C3AED);

  /// An emerald green colour for chart differentiation.
  static const Color chartEmerald = Color(0xFF059669);

  /// A cyan colour for chart differentiation.
  static const Color chartCyan = Color(0xFF0891B2);

  /// An amber colour for chart differentiation.
  static const Color chartAmber = Color(0xFFD97706);

  /// A blue colour for chart differentiation.
  static const Color chartBlue = Color(0xFF2563EB);

  /// A gold colour for the welcome page CTA button.
  static const Color ctaGold = Color(0xFFF8BC4B);

  /// A dark brown text colour for the welcome page CTA button.
  static const Color ctaGoldText = Color(0xFF271900);

  /// A dark amber border colour for the welcome page CTA button.
  static const Color ctaGoldBorder = Color(0xFF5F4100);

  /// The light theme primary colour.
  static const Color lightPrimary = Color(0xFF002045);

  /// The light theme primary container colour.
  static const Color lightPrimaryContainer = Color(0xFFD6E3FF);

  /// The light theme on-primary-container colour.
  static const Color lightOnPrimaryContainer = Color(0xFF001B3C);

  /// The light theme secondary colour.
  static const Color lightSecondary = Color(0xFF13696A);

  /// The light theme secondary container colour.
  static const Color lightSecondaryContainer = Color(0xFFA2EDED);

  /// The light theme on-secondary-container colour.
  static const Color lightOnSecondaryContainer = Color(0xFF002020);

  /// The light theme tertiary colour.
  static const Color lightTertiary = Color(0xFFCB9524);

  /// The light theme tertiary container colour.
  static const Color lightTertiaryContainer = Color(0xFF493100);

  /// The light theme on-tertiary-container colour.
  static const Color lightOnTertiaryContainer = Color(0xFFCB9524);

  /// The light theme error colour.
  static const Color lightError = Color(0xFFBA1A1A);

  /// The light theme error container colour.
  static const Color lightErrorContainer = Color(0xFFFFDAD6);

  /// The light theme on-error-container colour.
  static const Color lightOnErrorContainer = Color(0xFF93000A);

  /// The light theme surface colour.
  static const Color lightSurface = Color(0xFFF9F9FF);

  /// The light theme on-surface colour.
  static const Color lightOnSurface = Color(0xFF111C2C);

  /// The light theme elevated surface colour.
  static const Color lightSurfaceContainerHighest = Color(0xFFD8E3FA);

  /// The light theme on-surface-variant colour.
  static const Color lightOnSurfaceVariant = Color(0xFF43474E);

  /// The light theme outline colour.
  static const Color lightOutline = Color(0xFF74777F);

  /// The light theme outline-variant colour.
  static const Color lightOutlineVariant = Color(0xFFC4C6CF);

  /// The light theme inverse-surface colour.
  static const Color lightInverseSurface = Color(0xFF263142);

  /// The light theme inverse-primary colour.
  static const Color lightInversePrimary = Color(0xFFADC7F7);

  /// The dark theme primary colour.
  static const Color darkPrimary = Color(0xFFADC7F7);

  /// The dark theme on-primary colour.
  static const Color darkOnPrimary = Color(0xFF102F58);

  /// The dark theme primary container colour.
  static const Color darkPrimaryContainer = Color(0xFF2D476F);

  /// The dark theme on-primary-container colour.
  static const Color darkOnPrimaryContainer = Color(0xFFD6E3FF);

  /// The dark theme secondary colour.
  static const Color darkSecondary = Color(0xFF89D3D4);

  /// The dark theme on-secondary colour.
  static const Color darkOnSecondary = Color(0xFF003737);

  /// The dark theme secondary container colour.
  static const Color darkSecondaryContainer = Color(0xFF004F50);

  /// The dark theme on-secondary-container colour.
  static const Color darkOnSecondaryContainer = Color(0xFFA5EFF0);

  /// The dark theme tertiary colour.
  static const Color darkTertiary = Color(0xFFF8BC4B);

  /// The dark theme on-tertiary colour.
  static const Color darkOnTertiary = Color(0xFF3E2B00);

  /// The dark theme tertiary container colour.
  static const Color darkTertiaryContainer = Color(0xFF5F4100);

  /// The dark theme on-tertiary-container colour.
  static const Color darkOnTertiaryContainer = Color(0xFFFFDEAA);

  /// The dark theme error colour.
  static const Color darkError = Color(0xFFFFB4AB);

  /// The dark theme on-error colour.
  static const Color darkOnError = Color(0xFF690005);

  /// The dark theme error container colour.
  static const Color darkErrorContainer = Color(0xFF93000A);

  /// The dark theme on-error-container colour.
  static const Color darkOnErrorContainer = Color(0xFFFFDAD6);

  /// The dark theme surface colour.
  static const Color darkSurface = Color(0xFF0F172A);

  /// The dark theme on-surface colour.
  static const Color darkOnSurface = Color(0xFFEBF1FF);

  /// The dark theme elevated surface colour.
  static const Color darkSurfaceContainerHighest = Color(0xFF1E293B);

  /// The dark theme on-surface-variant colour.
  static const Color darkOnSurfaceVariant = Color(0xFFC4C6CF);

  /// The dark theme outline colour.
  static const Color darkOutline = Color(0xFF8E9099);

  /// The dark theme outline-variant colour.
  static const Color darkOutlineVariant = Color(0xFF43474E);

  /// The dark theme inverse-surface colour.
  static const Color darkInverseSurface = Color(0xFFEBF1FF);

  /// The dark theme inverse-primary colour.
  static const Color darkInversePrimary = Color(0xFF002045);
}
